# E2E test setup preset — functional tests, KWOK for test pods, no Pyroscope.

cluster:
  create: true
  worker_nodes: 0
  worker_memory: "150m"

kwok:
  nodes: 30
  node_cpu: "4"
  node_memory: "150Mi"

scheduler:
  kai:
    enabled: true

grove:
  enabled: true
  profiling: false
  podclique_topology_affinity: true
